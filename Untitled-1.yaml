echo "# steve-website-files" >> README.md
git init
git add .
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/mayor48/steve-website-files.git
git push -u origin main
git config --global user.email "stephenadeloye488@gmail.com"
  git config --global user.name "stephen"